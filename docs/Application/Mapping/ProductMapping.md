### ProductMapping

`ProductMapping` is a static mapping class responsible for converting a `Product` domain entity into a `ProductDto`.

#### Methods

* `ToDto(Product product)`: Converts a `Product` entity to a `ProductDto` by mapping the product's `Id`, `Name`, `Price`, and `Stock` properties.
